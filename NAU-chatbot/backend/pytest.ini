[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers
markers =
    unit: deterministic test with no external service
    integration: boundary test using in-process fakes or an explicitly configured service
